const ACTIONS = require('./actions');
const BOUNDARIES = require('./boundaries');
const ERROR_MESSAGES = require('./error-messages');
const REGEX_PATTERNS = require('./regex-patterns');
const VALUES = require('./values');

module.exports = {
  ACTIONS,
  BOUNDARIES,
  ERROR_MESSAGES,
  REGEX_PATTERNS,
  VALUES,
};
